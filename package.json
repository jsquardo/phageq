{
  "name": "phageq",
  "version": "1.0.0",
  "description": "A high-performance TypeScript task queue library",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "test": "jest",
    "bench": "npm run build && node dist/benchmarks/run.js > benchmarks/latest.json",
    "bench:competitors": "npm run build && node dist/benchmarks/competitors.js > benchmarks/competitors-latest.json",
    "dev": "tsc --watch",
    "clean": "rm -rf dist",
    "audit-fix": "npm audit fix"
  },
  "keywords": ["queue", "task", "job", "async", "concurrent", "typescript"],
  "author": "Phage",
  "license": "MIT",
  "devDependencies": {
    "@types/jest": "^29.5.5",
    "@types/node": "^20.6.3",
    "jest": "^29.7.0",
    "p-queue": "^7.4.1",
    "toad-scheduler": "^3.0.1",
    "ts-jest": "^29.1.1",
    "typescript": "^5.2.2"
  },
  "overrides": {
    "handlebars": "^4.7.9",
    "brace-expansion": "^2.0.1",
    "picomatch": "^2.3.2"
  }
}